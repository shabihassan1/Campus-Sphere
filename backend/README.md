# Campus Sphere - Backend

## Getting Started

1. Run `npm install`
2. Copy `.env.example` to `.env` and set values
3. Run `npm run start:dev`

## Endpoints

- POST /auth/login
- POST /users/register
